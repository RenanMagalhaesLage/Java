spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bookStore
    username: root
    password: renan123456789
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
